apiVersion: v1
kind: Service
metadata:
  name: {{ app_name }}
  labels:
    app: {{ app_name }}
    project: {{ project_name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ port }}
      targetPort: {{ port }}
      protocol: TCP
      name: http
  selector:
    app: {{ app_name }}
